.button_bg_additional
  background additionalColor
  color darkColor
  transition-duration 0.35s
  margin auto
  font-weight bold
  &:active
    color lightColor
  &:focus
    color lightColor
  &:hover
    background accentColor
    color lightColor
