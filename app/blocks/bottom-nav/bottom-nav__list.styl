.bottom-nav__list
  padding 0
  li
    padding 3px 0
    a
      color lightColor
      font-size round(fontSize * 0.8)
      &:hover
        color lightColor
        border-bottom 2px solid themeColor